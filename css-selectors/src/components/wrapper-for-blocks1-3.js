import {newElement} from './element-dom';
import {wrapper} from './wrapper';

export let wrapperForBlocks1_3 = newElement ({
    tag: 'div',
    params: {classList: ['wrapperForBlocks1_3']},
    parent: wrapper
});